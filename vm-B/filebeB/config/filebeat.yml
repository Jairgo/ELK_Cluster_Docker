#**************************************************************************
#filebeat.yml
#**************************************************************************

filebeat.inputs:
    - type: log
      enabled: true
      paths:
        - /home/jair2/covidB/filebeB/logs/logstash-tutorial.log

filebeat.config.modules:
  path: ${path.config}/modules.d/*.yml
  reload.enabled: false

setup.template.settings:
  index.number_of_shards: 1


output.logstash:
  hosts: ["40.84.129.164:9704"]

processors:
  - add_host_metadata: ~
  - add_cloud_metadata: ~
  - add_docker_metadata: ~
  - add_kubernetes_metadata: ~
